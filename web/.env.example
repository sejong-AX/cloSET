# cloSET web — 환경 변수 예시
# 실제 값은 .env.local (git 무시됨) 또는 Vercel 프로젝트 환경 변수에 저장합니다.

# OpenAI GPT-4o — 서버 라우트(app/api/*)에서만 사용, 클라이언트에 노출 금지
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 사용할 모델 (기본값: gpt-4o)
OPENAI_MODEL=gpt-4o
